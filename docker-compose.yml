services:
  vepay-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      VEPAY_API_MAX_CONCURRENCY: "2"
      VEPAY_API_MAX_FILE_SIZE_BYTES: "10485760"
      VEPAY_API_ENABLE_UI: "false"
      VEPAY_API_REQUIRE_API_KEY: "false"
